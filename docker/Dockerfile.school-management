# Runtime-only image (avoids pulling maven from Docker Hub).
# Build the JAR on the host first (see docker/up.ps1).
FROM eclipse-temurin:25-jre
WORKDIR /app
COPY target/School-Management-0.0.1-SNAPSHOT.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
